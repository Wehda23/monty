#include "monty.h"




/**
 * swap - swaps the top two elements of the stack.
 * @stack: pointer to a stack
 * @line_number: Line number 
 */
void swap(stack_t **stack, unsigned int line_number)
{
    printf("%d%d", (*stack)->n, line_number);
}